name: Test
on:
  push:
    branches: [main]
    paths-ignore:
      - "bigearthnet_gdf_builder/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "bigearthnet_gdf_builder/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
